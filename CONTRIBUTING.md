# Contributing

Edit the readable files directly:

```text
bootstrap-pickadate.js
bootstrap-pickadate.css
locales/bootstrap-pickadate.*.js
```

Then run:

```bash
npm run build
```

The generated `.min` files should be committed with the readable files.
